jQuery(document).ready(function($) {
  // Correct value for input form.
  $('.sample-details-buttons a').attr('href', $('.sample-details-buttons a').attr('href').replace('/enter-app-record', '/mydata/samples/edit'))
});